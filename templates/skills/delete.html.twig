<form method="post" action="{{ path('app_skills_delete', {'id': skill.id}) }}" onsubmit="return confirm('Es-tu sur(e) de vouloir supprimer la compétence {{ skill.name }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.id) }}">
    <button class="pf-button-delete">Supprimer</button>
</form>