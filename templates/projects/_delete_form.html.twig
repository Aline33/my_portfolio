<form method="post" action="{{ path('app_projects_delete', {'id': project.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer le projet {{ project.name }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
    <button class="pf-button-delete">Supprimer</button>
</form>
